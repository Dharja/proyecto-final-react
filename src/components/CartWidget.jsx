import React from 'react';

const CartWidget = () => {
// Lógica del carrito
    return (
        <div>
        {/* Cart icon */}
        {/* Cart item count */}
        </div>
    );
};

export default CartWidget;
